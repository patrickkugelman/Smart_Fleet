# Application
spring.application.name=smart-fleet-tracking
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/fleet2
spring.datasource.username=postgres
spring.datasource.password=UTcn

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=SmartFleetSecretKeyForJWT2024ThisIsAVeryLongSecretKeyThatShouldBeKeptSecure
jwt.expiration=86400000

# === EMAIL CONFIGURATION (GMAIL) ===
spring.mail.host=smtp.gmail.com
spring.mail.port=587

# Adresa de pe care se trimit emailurile (Expeditorul)
spring.mail.username=patrick12.kugelman@gmail.com

# Parola de Aplicatie (App Password) - FARA SPATII
spring.mail.password=gysdwfkehjpiqxih

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Adresa managerului (unde se trimit alerte, poate fi tot adresa ta)
app.fleet.manager.email=patrick12.kugelman@gmail.com

# === SPRING AI / OLLAMA CONFIG ===
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.model=llama3
spring.ai.ollama.chat.options.temperature=0.7

# Logging
logging.level.root=INFO
logging.level.com.smartfleet=DEBUG
logging.level.org.springframework.security=DEBUG
server.servlet.context-path=/